<UserControl
    x:Class="SerialPortTestGuiApp.Views.MotorView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:cmd="clr-namespace:SerialPortTestGuiApp.Commands"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="450"
    d:DesignWidth="800"
    Background="White"
    mc:Ignorable="d">
    <StackPanel Width="300" VerticalAlignment="Center">
        <TextBlock Text="LS micro firmware version" />
        <TextBox IsReadOnly="True" Text="{Binding LsFirmware}" />
        <TextBlock Margin="0,5,0,0" Text="HS micro firmware version" />
        <TextBox IsReadOnly="True" Text="{Binding HsFirmware}" />
    </StackPanel>
</UserControl>
